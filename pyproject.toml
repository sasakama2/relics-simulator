[project]
name = "relics-simulator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pytest>=8.4.2",
]

[dependency-groups]
dev = [
    "ruff>=0.14.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/my_project"]